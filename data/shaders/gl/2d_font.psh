#version 330 core

in vec2 v_texcoord;
in vec4 v_color;

out vec4 fragColor;

uniform sampler2D u_texture;

void main()
{
	float a = texture(u_texture, v_texcoord).r;
	fragColor = v_color * vec4(1.0f, 1.0f, 1.0f, a);
}
